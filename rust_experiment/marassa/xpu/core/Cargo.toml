[package]
name = "xpu-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

description = "core traits and types for device-agnostic heterogeneous computing"

[lib]
name = "xpu_core"
path = "src/lib.rs"

[features]
default = []
std = []

[dependencies]
# no dependencies - pure trait definitions

[dev-dependencies]
