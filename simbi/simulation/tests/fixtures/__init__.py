# =============================================================================
# tests/fixtures
#
# constrained-transport div(B) probe configs used ONLY by the GRMHD test gates —
# advected poloidal field loops on the curved charts (spherical / kerr / cylindrical
# (R, z) / (R, phi) disk) and the kerr rotating-equilibrium check. these are TEST
# FIXTURES: a "field loop" is a cartesian advection benchmark, so on a curved
# chart it is a div(B)-preservation probe.
# the physics demos (michel, bondi, fishbone-moncrief, the cartesian field loop) live
# in simbi_configs/examples/grmhd/.
# =============================================================================
